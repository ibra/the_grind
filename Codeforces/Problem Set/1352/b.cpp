#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main()
{
    ll t;
    while (t--)
    {
        ll n, k;
        cin >> n >> k;

        if (n % 2 == 0)
        {
        }
        else
        {
        }
    }
}